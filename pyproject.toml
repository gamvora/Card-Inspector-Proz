[project]
name = "lucewebsitechk"
version = "0.1.0"
description = "Web App and Telegram Bot"
dependencies = [
    "flask",
    "python-telegram-bot",
    "requests",
    "gunicorn"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
